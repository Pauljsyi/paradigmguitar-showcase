import React from 'react'

const Summary = (props) => {
    const { cartItems } = props
    console.log("cartItems", cartItems)
    return (
        <div>
            SUMMARY
        </div>
    )
}

export default Summary
